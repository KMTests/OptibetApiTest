default:
    autoload: [ %paths.base%/features/bootstrap ]
    suites:
        default:
            paths:    [ %paths.base%/features/api ]
            filters:
            contexts:
                - FeatureContext